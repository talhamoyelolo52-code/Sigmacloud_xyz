module.exports = {
    name: 'interactionCreate',
    
    async execute(interaction, client) {
        // Handle slash commands if needed in future
        if (interaction.isChatInputCommand()) {
            // Future slash command handler
        }
    }
};
